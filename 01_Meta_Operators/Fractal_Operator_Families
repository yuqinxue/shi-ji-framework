元算子库 (Meta-Operators Library)
定位：核心公理与第一性原理应用之间的“工程化中间件”
1. 核心定位：元胚 (Meta-Embryo)
本目录并非普通的应用实例，而是势迹框架的工程化元胚。
性质：递归可嵌套、可复用、可组合的算子集合。
功能：将 00_Core_Axioms 中的抽象公理（A1-A6），转化为具备张量定义、接口规范和计算逻辑的可执行协议。
目标：为复杂系统（物理、意识、市场等）提供统一的拓扑几何语言。
2. 基础约束（硬编码规则）
本库中所有算子的构建，必须遵循以下不可更改的底层约束：
2.1 384 爻形唯一性 (Axiom A6)
铁律：所有算子的最终投影基底均为 384维离散势态空间 (S384)。
依据：该维度由 (Z/2)6×Z/6（六十四卦 × 六爻）结构唯一确定。任何试图修改此维度的操作均被视为破坏框架根基。
表现：所有核心算子矩阵（如 Mrule,Mobs）的维度定义均为 384×384。
2.2 分形质数维度 (Fractal Prime Dimensions)
算子在 384 基底之上，通过悬挂纤维层（Fibers）实现全息嵌套。当前定义的层级结构为：
p=2 (阴阳法)：内部维度，承载二元对立与统一。
p=3 (三才法)：内部维度，承载结构稳定性。
p=5 (五蕴法)：悬挂纤维，承载系统涌现属性。
p=7 (七位法)：悬挂纤维，承载自指统摄与周期节律（七日来复）。
p=11 (十一律法)：【扩展层】​ 位于 p=7 纤维内部（Mrule(7)⊗M11law）。
3. 核心算子族 (Fractal Operator Families)
3.1 双流形耦合算子 (Mrule/Mobs)
定义：定义势态演化的规则侧与观测侧。
接口：
Input: 离散势态 St∈S384，质数维度 p。Output: 变换后的势态 St+1，残差向量 E。
递归逻辑：高层算子（如 p=7）可调用底层算子（如 p=2）作为其内核。
3.2 残差向量 (E∈R4)
定义：系统拓扑摩擦的诊断指标。
分量：
E(2),E(3)：基础结构摩擦。
E(5)：涌现层摩擦。
E(7)：自指统摄摩擦（含十一律细分残差）。
用途：预警“势迹永断”及相变临界点。
3.3 丛焰自举机制 (Congyan Self-Bootstrapping)
触发条件：当 p=7 维度满足特定拓扑缠绕数时，系统自发启动逻辑闭环。
边界限制：受限于 137（物理阈值）与 163（逻辑边界）的双重约束。
4. 复用与递归指南
本库设计为可编程的递归结构：

